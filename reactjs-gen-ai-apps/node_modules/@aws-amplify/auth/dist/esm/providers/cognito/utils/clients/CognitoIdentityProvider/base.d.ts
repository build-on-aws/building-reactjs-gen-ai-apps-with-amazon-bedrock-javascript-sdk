import { Endpoint, EndpointResolverOptions, Headers, HttpRequest, HttpResponse } from '@aws-amplify/core/internals/aws-client-utils';
/**
 * A Cognito Identity-specific transfer handler that does NOT sign requests, and
 * disables caching.
 *
 * @internal
 */
export declare const cognitoUserPoolTransferHandler: (request: HttpRequest, options: import("@aws-amplify/core/internals/aws-client-utils").UserAgentOptions & import("@aws-amplify/core/internals/aws-client-utils").RetryOptions<HttpResponse> & import("@aws-amplify/core/internals/aws-client-utils").HttpTransferOptions) => Promise<HttpResponse>;
/**
 * @internal
 */
export declare const defaultConfig: {
    service: string;
    endpointResolver: ({ region }: EndpointResolverOptions) => {
        url: URL;
    };
    retryDecider: (response?: HttpResponse | undefined, error?: unknown) => Promise<boolean>;
    computeDelay: (attempt: number) => number;
    userAgentValue: string;
    cache: string;
};
/**
 * @internal
 */
export declare const getSharedHeaders: (operation: string) => Headers;
/**
 * @internal
 */
export declare const buildHttpRpcRequest: ({ url }: Endpoint, headers: Headers, body: string) => HttpRequest;
